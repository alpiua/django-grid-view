# Planner Repository Add-on

- `repository`: `grid-view-spec`
- `planner_root`: `../../planner`
- `plans`: `grid-view-spec/plans`
- `roadmap`: none
- `mempalace_wing`: `planner-grid-view-spec`
- `documentation`: `docs/**/*.md`
- `linear`: false

Use the current Git root as the docs checkout and `CONTEXTUNITY_PLANNER_ROOT`
(default `../../planner`) as an independent Planner checkout. Submit separate
changed sets and search only wing `planner-grid-view-spec`.
