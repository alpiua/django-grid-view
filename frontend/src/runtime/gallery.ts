/** Gallery block runtime — reserved for vNext gallery init (no-op until host wiring). */
export function initGalleryBlocks(_scope: Document | Element = document): void {
  /* vNext GridViewGallery: host pages may extend via GridView.registerRenderer. */
}
