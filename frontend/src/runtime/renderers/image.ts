/** Image renderer runtime — reserved for vNext image column renderer registration. */
export function initImageRenderers(_scope: Document | Element = document): void {
  /* Host-specific image renderers register through GridView.registerRenderer. */
}
