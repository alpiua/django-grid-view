#!/usr/bin/env bash
# Release gate: formatting, linting, strict typing, and tests.
set -euo pipefail

uv run ruff format --check .
uv run ruff check .
uv run basedpyright --warnings src/grid_view_spec tests
uv run pytest
